export interface INewWorkspacePermissionsProps {}

export enum ENWPermissionsType {
  AUTO_CLOSE_WORKSPACE = 'Auto-close workspace',
  ENFORCE_PEER_LIMIT = 'Enforce peer limit',
  ADDITIONAL_WORKSPACE_OWNERS = 'Additional workspace owners'
}